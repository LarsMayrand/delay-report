delay_report_notes

# print(station_delays_reasons)
    # pie_chart(dict(station_delays_reasons))
    # pie_chart_fancy(dict(format_for_pie_chart(station_delays_reasons, 9)))
    # pie_chart_fancy(dict(station_delays_reasons))
    
    # fleet_numbers = fleet_breakdown(data)
    # fleet_numbers = fleet_breakdown(filter(data, is_cg_delay))
    # pie_chart_fancy(fleet_numbers)
    # # print(type(fleet_numbers))
    
    # wb = filter(data, is_widebody)
    # delays = filter(data, is_late_departure)
    # print(len(delays))
    # stars = filter(data, is_star)
    # print(len(stars))
    # cg_delays = filter(data, is_cg_delay)
    # cg_info = info(cg_delays)
    # print(info(data))
    # print(desinations_info(data))
    # plot_station_reasons(data)